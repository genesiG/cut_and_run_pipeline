# CLAUDE.md

CRITICAL: Always request an interactive node (`nodeXXX`) to run the code, if you are not already in one.
Use `hostname` to check if you are in an interactive node.
Use `bsub -Is bash` to request an interactive node.

Read `AGENTS.md` for the full agent constitution.

> This file exists so the project works unchanged if you switch from  
> Google Antigravity to another Claude-based agent runner (e.g. Claude Code).
